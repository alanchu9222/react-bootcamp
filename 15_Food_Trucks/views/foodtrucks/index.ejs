<% include ../partials/header %>

<div class="container">
  <!-- <% if(!currentUser){ %>
  <header class="jumbotron">
    <div class="container">
      <h1 class="welcome">
        Welcome Food Hunter!
      </h1>
    </div>
  </header>
  <% } %> -->

  <div class="row text-center" style="display:flex; flex-wrap:wrap;">
    <div class="col-md-3 col-sm-6" style="margin:auto;">
      <form action="" method="GET" onchange="submit()">
        <select class="form-control" name="searchKey">
          <% categories.forEach((cat)=>{%>
          <option value="<%=cat%>"><%=cat%></option>
          <%}) %>
        </select>
      </form>
    </div>
  </div>

  <div class="row text-center" style="display:flex; flex-wrap:wrap;">
    <% foodtrucks.forEach(function(foodtruck) { %>
    <div class="col-md-3 col-sm-6 img-frame">
      <div class="img-thumbnail img">
        <a class="caption" href="/foodtrucks/<%=foodtruck._id%>">
          <img class="img-thumbnail" src="<%=foodtruck.image%>" />
          <%=foodtruck.name%></a
        >
      </div>
    </div>
    <% });%>
  </div>
</div>
<% include ../partials/footer %>
